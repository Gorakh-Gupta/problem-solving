class Solution {
    public int mirrorDistance(int num) {

        int n = nums, reversed = 0;
        while (num != 0) {
            int digit = num % 10;
            reversed = reversed * 10 + digit;
            num /= 10;
        }
        return Math.abs(n - reversed);
    }
}
