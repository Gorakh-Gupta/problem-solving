class Solution {
public:
    int repeatedNTimes(vector<int>& nums) {
        int n = nums.size();
        for (int i=1;i<n;i++) {
            if(nums[i] == nums[i-1])return nums[i];
        }
        if(nums[0]==nums[n-2]) return nums[0];
        return nums[n-1];
    }
};
